<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.omar.contactbook">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.SplashScreen.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.Category.CategoryActivity" />
        <activity android:name=".ui.Login.LoginActivity" />
        <activity android:name=".ui.OnBoarding.OnBoardingActivity" />
        <activity android:name=".ui.MainMenu.MainMenuActivity" />
        <activity android:name=".ui.CategorySetting.CategorySettingActivity" />
        <activity android:name=".ui.AddContact.AddContactActivity" />
        <activity android:name=".ui.Contact.ContactActivity"></activity>
    </application>

</manifest>